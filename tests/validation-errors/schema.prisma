// ========================
// Generator Tests
// ========================

generator typescriptInterfaces {
  provider           = "tsx src/generator.ts"
  modelType          = "bad"
  enumType           = incorrect
  relations          = something
  counts             = no
  typeImportPath     = [["nested"], type]
  intType            = ["wrong"]
  wrongOption        = ["an", option, ""]
  stringType         = []
  booleanType        = ""
  badOption          = "not"
  exportEnums        = "this"
  optionalNullables  = "a"
  optionalDefaults   = "b"
  prettier           = true
  prettierConfigPath = "/invalid/path!"
}

// ========================
// Prisma Schema
// ========================

datasource db {
  provider = "postgresql"
  url      = ""
}

model User {
  id Int @id
}
