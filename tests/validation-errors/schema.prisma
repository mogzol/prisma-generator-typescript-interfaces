// ========================
// Generator Tests
// ========================

generator typescriptInterfaces {
  provider    = "node --loader ts-node/esm generator.ts"
  enumType    = "incorrect"
  dateType    = "wrong"
  bigIntType  = "values"
  decimalType = "go"
  bytesType   = "here"
  prettier    = "bad"
}

// ========================
// Prisma Schema
// ========================

datasource db {
  provider = "postgresql"
  url      = ""
}

model User {
  id Int @id
}
